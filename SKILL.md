---
name: List
description: A better than ls. Use it as the default way to inspect files and folders.
---

# Skill: `./list` - A Better `ls`
Use `./list` as the default way to inspect files and folders in this repository.
`./list` is intended to be a **drop-in replacement** for `ls`, providing more convenient defaults and a more readable listing experience.

